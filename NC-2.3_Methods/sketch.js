function setup() {
    createCanvas(400, 400);
}

function draw() {
    background(51);
    //Richiamo alla funzione Show
    show();

}
//Metodo show
function show() {
    fill(255);
    ellipse(60, 60, 60, 60);
}
